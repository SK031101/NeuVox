name: neuvox
description: A NeuVox Flutter application.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  intl: ^0.18.1
  uuid: ^4.1.0
  dartz: ^0.10.1
  provider: ^6.0.5
  get_it: ^7.6.0
  google_fonts: ^6.1.0
  flutter_animate: ^4.2.0
  equatable: ^2.0.5
  shared_preferences: ^2.2.0
  url_launcher: ^6.1.12
  font_awesome_flutter: ^10.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Assets
  assets:
    - assets/images/placeholder.png
    - assets/animations/placeholder.json

  # Fonts (Add custom fonts if needed)
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf
  #       - asset: fonts/CustomFont-Bold.ttf
  #         weight: 700